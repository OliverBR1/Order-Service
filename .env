# Variáveis de ambiente para o docker-compose
# Altere os valores antes de usar em produção!
PGUSER=postgres
PGPASSWORD=admin123

# --- Kafka (deixe em branco para usar o padrão localhost:9092) ---
# KAFKA_BOOTSTRAP_SERVERS=localhost:9092

# --- Perfil Spring (dev | prod) ---
# SPRING_PROFILES_ACTIVE=dev


